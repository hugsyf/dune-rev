<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:sys="clr-namespace:System;assembly=mscorlib" mc:Ignorable="d">
    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlIdleBorderBrush}" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid x:Name="Root">
                        <Border x:Name="HoverBg" Background="{DynamicResource ControlIdleBackgroundBrush}" CornerRadius="{DynamicResource ControlCornerRadiusSmall}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" />
                        <Border Name="DuneBottomStroke" Height="1" Margin="4,0,4,0" VerticalAlignment="Bottom" Background="{DynamicResource TextBrushDarker}" Opacity="0.5" />
                        <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <DockPanel LastChildFill="False" Background="Transparent">
                                        <Path DockPanel.Dock="Right" Width="8" Height="5" Margin="0,0,12,0" VerticalAlignment="Center"
                                              Data="M 0,0 L 4,4 L 8,0" Stroke="{DynamicResource TextBrush}" StrokeThickness="1.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                    </DockPanel>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0,28,0" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                        <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="8,0,28,0" Focusable="True" BorderThickness="0" Background="Transparent" IsReadOnly="{TemplateBinding IsReadOnly}" Padding="0">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <ScrollViewer Name="PART_ContentHost" />
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                        <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" Focusable="False" AllowsTransparency="True" VerticalOffset="4">
                            <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <Border Style="{DynamicResource PopupBorder}" CornerRadius="{DynamicResource ControlCornerRadius}" />
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Padding="4">
                                    <ItemsPresenter Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter Property="Visibility" Value="Visible" TargetName="PART_EditableTextBox" />
                            <Setter Property="Visibility" Value="Hidden" TargetName="ContentSite" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HoverBg" Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}" />
                        </Trigger>
<Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="DuneBottomStroke" Property="Background" Value="{DynamicResource AccentBrush}" /><Setter TargetName="DuneBottomStroke" Property="Height" Value="2" /><Setter TargetName="DuneBottomStroke" Property="Opacity" Value="1" /></Trigger>
<Trigger Property="IsDropDownOpen" Value="True"><Setter TargetName="HoverBg" Property="Background" Value="{DynamicResource ControlSelectedBackgroundBrush}" /><Setter TargetName="DuneBottomStroke" Property="Background" Value="{DynamicResource AccentBrush}" /><Setter TargetName="DuneBottomStroke" Property="Height" Value="2" /><Setter TargetName="DuneBottomStroke" Property="Opacity" Value="1" /></Trigger></ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="12,0,12,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Grid Height="32" x:Name="Root">
                        <Border x:Name="HoverBg" IsHitTestVisible="False" CornerRadius="{DynamicResource ControlCornerRadiusSmall}" Opacity="0" Background="{DynamicResource ControlOverlayBrush}" />
                        <Border Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <Border Name="SelectionIndicator" Width="3" Height="16" CornerRadius="1.5" HorizontalAlignment="Left" VerticalAlignment="Center" Background="{DynamicResource AccentBrush}" Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="HoverBg" Property="Opacity" Value="0.08" />
                            <Setter TargetName="SelectionIndicator" Property="Visibility" Value="Visible" />
                        </Trigger>
<Trigger Property="IsHighlighted" Value="True"><Setter TargetName="HoverBg" Property="Opacity" Value="0.12" /></Trigger>
<Trigger Property="IsMouseOver" Value="True"><Setter TargetName="HoverBg" Property="Opacity" Value="0.12" /></Trigger></ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers></Style.Triggers>
    </Style>
    <Style x:Key="ExplorerComboBox" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type ComboBox}}"></Style>
    <!-- ThemeExtras completion-status editor, styled with Dune Rev's normal ComboBox. -->
    <Style x:Key="ThemeExtrasUserControlCompStatusStyle" TargetType="UserControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <ComboBox MinWidth="115" MaxWidth="180" Margin="-8,0,0,0" FontSize="{TemplateBinding FontSize}" ItemsSource="{Binding CompletionStatusOptions}" SelectedItem="{Binding Value}" Height="28">
                        <ComboBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type CompletionStatus}">
                                <TextBlock Text="{Binding Name}" FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=ComboBox}}" FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=ComboBox}}" FontWeight="Normal" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
